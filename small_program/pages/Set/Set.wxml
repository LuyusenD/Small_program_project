<!--pages/Set/Set.wxml-->
<van-tabs active="{{ active }}" bind:change="activeonChange">
  <van-tab title="{{language?'添加':'add'}}"></van-tab>
  <van-tab title="{{language?'删除':'del'}}"></van-tab>
</van-tabs>
<view wx:if="{{active==0}}" style='height:100%'>
  <view style='height:50rpx;'></view>
  <view class='ioic'>
    <image bindtap='base64' src="{{img=='/image/add.png'?img:url+img}}"></image>
  </view>
  <view style='clear:both; margin-top:10%'>
    <van-button size="large" bindtap='addoType' type='primary'>{{language?'上传图片':'Upload pictures'}}</van-button>
  </view>
</view>
<block wx:if="{{active==1}}">
  <!-- <van-swipe-cell wx:for="{{serve}}" wx:key="{{index}}"  id="swipe-cell" right-width="{{ 65 }}"  async-close bind:close="onClose">
    <van-cell-group >
    <image bindtap='set' data-detail='{{item}}' style='width:80rpx;height:80rpx;margin-left:4%' src='{{url+item.url}}'></image>
    </van-cell-group>
    <view slot="right" data-detail='{{item}}' bindtap='Del'>删除</view>
  </van-swipe-cell> -->
<van-row wx:for="{{serve}}" wx:key="{{index}}">
  <van-col span="8"> 
   <image  data-detail='{{item}}' style='width:200rpx;height:200rpx;margin-left:4%' src='{{url+item.url}}'></image>
  </van-col>
  <van-col offset="12" span="4" span="4">
  <button size='mini' data-detail='{{item}}' bindtap='Del' style='margin-top:60rpx;'>{{language?'删除':'Del'}}</button>
  </van-col>
</van-row>
    <!-- <van-cell-group wx:for="{{serve}}" wx:key="{{index}}">
   
    <van-col span="16"></van-col>
    <button size='mini'>删除</button>
    </van-cell-group> -->
    <!-- <view slot="right" data-detail='{{item}}'  bindtap='Del'>删除</view> -->
</block>
  
<van-dialog id="van-dialog" />